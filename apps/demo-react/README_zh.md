# weave-demo-react

React 演示：由 React 控制数据与表单，右侧分屏展示两种场景构建方式（`sceneFromJSX` vs `@jiujue/weave-types` JSX runtime）。

## 运行

在仓库根目录：

```bash
pnpm install
pnpm dev:react
```

## 相关包

- `@jiujue/weave-app`
- `@jiujue/weave-react`
- `@jiujue/weave-types`
