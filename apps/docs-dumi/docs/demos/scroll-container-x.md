---
title: 横向滚动容器（Container）
order: 57
toc: content
---

当 `container` 的宽度被 `width/maxWidth` 限制且内容超出时，可以用 `overflowX` + `scroll.x` 做横向滚动，同时会在底部渲染滚动条。

<code src="../../src/demos/scroll-container-x-canvas.tsx" title="横向滚动容器示例（maxWidth + overflowX + updateScroll）"></code>

