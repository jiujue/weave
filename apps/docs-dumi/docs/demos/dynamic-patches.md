---
title: 动态更新（Patch）
order: 40
toc: content
---

用 `applyPatches` 做增量更新，并在每次更新后 `render → replay` 产出画面。

<code src="../../src/demos/dynamic-patches-canvas.tsx" title="动态更新：applyPatches + render + replay"></code>
