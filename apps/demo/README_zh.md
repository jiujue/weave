# weave-demo

浏览器演示：主线程 UI + Worker（OffscreenCanvas）渲染，展示两种场景构建方式的对照。

## 运行

在仓库根目录：

```bash
pnpm install
pnpm dev
```

## 相关包

- `@jiujue/weave-app`
- `@jiujue/weave-react`
- `@jiujue/weave-types`
